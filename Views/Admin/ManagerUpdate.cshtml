
@{
    ViewBag.Title = "ManagerUpdate";
}

<form action="/Admin/ManagerUpdate" method="post">
    <div class="row">
        <div class="col-xs-1 col-sm-1 col-md-3 col-lg-3"></div>
        <div class="col-xs-10 col-sm-10 col-md-5 my-5">

            <h3>New manager add</h3>

            <div class="py-3">
                <label for="inputEmailForSignin" class="form-label">Email</label>
                <input type="email" class="form-control" name="inputEmailForSignin" id="inputEmailForSignin" required minlength="0">
            </div>

            <div class="py-3">
                <label for="inputPasswordForSignin" class="form-label">Password</label>
                <input type="password" class="form-control" name="inputPasswordForSignin" required minlength="0">
            </div>
        </div>

        <div class="col-xs-10 col-sm-10 col-md-1 my-5">
            <button type="submit" class="btn btn-primary" name="buttonForSignin">Add</button>
        </div>

        <div class="col-xs-1 col-sm-1 col-md-3"></div>
    </div>
</form>

<br>
<br>
<br>

<div class="table-responsive row mx-0 px-0 pt-3">
    <div class="col-xs-1 col-sm-1 col-md-1 col-lg-1"></div>

    <div class="col-xs-10 col-sm-10 col-md-10 col-lg-10 py-2">

        <div class="row">
            <div class="form-inline px-3 py-3 bg-info">
                <input type="text" class="form-control rounded-pill" name="search" id="search_text" placeholder="Search here...">
            </div>
        </div>

        <div class="row">

            <?php
            $query = "SELECT * FROM `book_records` JOIN `books` on `book_records`.`book_id`=`books`.`id`";
            $result = mysqli_query($con, $query);
            ?>

            <table class="table table-hover table-bordered" id="table_data">
                <thead>
                    <tr>
                        <th scope="col"><h5>ISBN</h5></th>
                        <th scope="col"><h5>Book name</h5></th>
                        <th scope="col"><h5>Author name</h5></th>
                        <th scope="col"><h5>Book price</h5></th>
                        <th scope="col"><h5>Book description</h5></th>
                        <th scope="col"><h5></h5></th>
                        <th scope="col"><h5></h5></th>
                    </tr>
                </thead>

                <tbody>
                    <?php

                    if (mysqli_num_rows($result) > 0)
                    {
                    while ($books_data = mysqli_fetch_assoc($result))
                    {
                    $query = "SELECT * FROM author WHERE id=".$books_data["author"];
                    $author_res = mysqli_query($con, $query);
                    $author_data = mysqli_fetch_assoc($author_res);

                    ?>
                    <tr>
                        <td><?php echo $books_data['id']; ?></td>
                        <td><b><?php echo $books_data['name']; ?></b></td>
                        <td><?php echo $author_data['name']; ?></td>
                        <td><?php echo $books_data['price_sell']; ?></td>
                        <td><?php echo $books_data['description']; ?></td>
                        <td><button id="edit_book_button" type='button' onclick="edit_clicked('<?php echo $books_data['id'] ?>')" class='btn btn-info' name='<?php echo $books_data[' id'] ?>' value="<?php echo $books_data['id'] ?>">Edit</button></td>
                        <td><a href='delete_book.php?delete_isbn=<?php echo $books_data[' id'] ?>'><button type='button' class='btn btn-danger' name='deleteButton'>Delete</button></a></td>
                    </tr>
                    <?php
                    }
                    }
                    ?>
                </tbody>
            </table>
        </div>
    </div>

    <div class="col-xs-1 col-sm-1 col-md-1 col-lg-1"></div>
</div>
